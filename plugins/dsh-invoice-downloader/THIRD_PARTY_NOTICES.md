# Third-party notices

This bundle is distributed under Apache-2.0 together with the source project notice in [NOTICE](NOTICE).

At first-run installation, the local Python runtime installs the pinned dependencies named in `runtime/requirements-headless.txt`. Those projects retain their own licenses and notices. The runtime also uses the DeepSeek Harness APIs supplied by the host installation; DeepSeek Harness is not bundled into this package.

No invoice, mailbox content, credential, DSH profile, browser profile, or generated scan output is included in the npm package.
